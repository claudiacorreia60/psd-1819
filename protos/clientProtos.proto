syntax = "proto2";

message Message {
    optional string type = 1; // Bid, Subscription, Auction, Emission 
    optional int32 amount = 2;
    optional int32 interest = 3; 
    optional Authentication auth = 4
    optional Result res = 5
    optional Notification not = 6;
    optional bytes pid = 7;
}

message Authentication {
  optional string username = 1;
  optional string password = 2;
}

message Result {
    optional bool result = 1;
    optional string entity = 2;
}

message Notification {
	optional string status = 1;
	optional string action = 2;
}
